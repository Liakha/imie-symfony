{% extends 'base.html.twig' %}

{% block body %}
    <h1>Link creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        {#{{ form_label(form.name) }}#}
        {#{{ form_widget(form.name, {'attr': {'data-id':'test'}}) }}#}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('link_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}